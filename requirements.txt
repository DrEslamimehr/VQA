# QA-TM reproduction environment.
# Versions pinned to match Table 1 of the paper where applicable.
# Tested on Python 3.11.4 (paper) and Python 3.12 (this bundle's CI).

# --- Core scientific stack ---
numpy==1.26.4
scipy>=1.11,<2.0
scikit-learn>=1.3
PyYAML>=6.0
matplotlib>=3.7

# --- Quantum simulation (paper: PennyLane 0.38.0, default.qubit) ---
pennylane==0.38.0
autoray==0.6.12

# --- Classical DL / tensor backend (paper: PyTorch backend) ---
torch>=2.1

# --- Differential privacy (paper: Opacus 1.4.0). Optional at runtime;
#     a self-contained Laplacian DP mechanism is bundled as a fallback. ---
opacus==1.4.0 ; python_version < "3.13"

# --- Testing ---
pytest>=7.4
